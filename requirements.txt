# Core LLM
anthropic>=0.40.0
httpx>=0.27           # already pulled by anthropic, but we use it directly in src/http_util.py (shared by sports/weather/games/tmdb)

# Wake-word detection (local, MIT licensed)
openwakeword>=0.6.0

# Speech-to-text (CTranslate2-backed Whisper, CPU-friendly)
faster-whisper>=1.0.0

# Text-to-speech
edge-tts>=6.1.0       # primary: Microsoft online voices
pyttsx3>=2.90         # fallback: offline Windows SAPI

# Audio I/O
sounddevice>=0.4.6
numpy>=1.24
miniaudio>=1.59     # MP3 decoder (edge-tts returns mp3) — pure pip, no ffmpeg needed

# System tray icon
pystray>=0.19.5
Pillow>=10.0          # required by pystray for icon rendering

# Console window (dark-themed Tk wrapper)
customtkinter>=5.2.0

# Config
python-dotenv>=1.0.0

# MCP — Model Context Protocol client + Plex MCP server (M21)
# We spawn plex-mcp-server as a stdio subprocess and talk to it via the mcp
# SDK's ClientSession. plex-mcp-server pulls in plexapi as its transitive dep.
mcp>=1.0.0
plex-mcp-server>=1.1.7

# PC diagnostics + system control (M23) — local Windows telemetry and
# allowlisted actions. psutil for CPU/RAM/disk/processes/services. pycaw for
# the Windows Audio Endpoint (system master volume). pywin32 NOT required —
# we shell out to PowerShell Get-WinEvent for event log queries.
psutil>=5.9
pycaw>=20240210

# Remote Plex laptop diagnostics over SSH (M24). paramiko is pure-Python
# (depends on cryptography for asymmetric crypto). Used for log triage and
# remote system-health queries against the Plex laptop using ed25519 key auth.
paramiko>=3.4

# News headlines (M47) — get_news tool. feedparser is small, pure-Python
# (no transitive deps), and robust across RSS 2.0 / Atom / RDF + malformed
# real-world feeds; it never raises (sets a bozo flag), matching Jarvis's
# fail-soft contract. Feed bytes are fetched via the shared src/http_util.py
# (first new consumer of the M43 extraction); feedparser only parses them.
feedparser>=6.0

# Remote console (M48) — LAN-only phone/web console. Pure-Python, no heavy
# transitive deps; runs its own asyncio loop in a daemon thread (the app is
# threaded). stdlib has no WS server, so a dep is genuinely required (the
# M47 dep-asymmetry principle); `websockets` is the minimal robust choice
# over aiohttp/FastAPI, and serves the one-page PWA from its HTTP handler.
websockets>=13.0

# Camera vision (M29) — grab a still frame from the USB webcam for the
# camera_snapshot tool, which hands the image to Claude (multimodal) to
# describe. headless = no GUI/Qt bits we don't need. cv2.VideoCapture also
# does RTSP, so this is forward-compatible if IP cameras get added later.
opencv-python-headless>=4.8

# Security mode (M34) — local person detection via YOLOv8n. Pulls in torch
# CPU (~200 MB) + non-headless opencv-python as transitive deps. YOLO model
# (yolov8n.pt, ~6 MB) auto-downloads to ~/.cache/torch/hub on first inference.
# CPU inference at ~150-300ms per frame on a Ryzen 5 3400G — fits the 0.5Hz
# polling rate at <10% one-core utilization. MIT licensed.
ultralytics>=8.4

# Knowledge-base semantic search (M46) — local sentence embeddings for the
# knowledge_search hybrid retrieval (keyword FTS5 + vector, fused by RRF).
# torch (the heavy part) is ALREADY pulled by ultralytics above, so this is
# an incremental add, not a fresh ~200 MB dep. Model all-MiniLM-L6-v2
# (384-dim, ~80 MB) auto-downloads to the HF cache on first reindex. LOCAL by
# requirement — the private corpus must never leave the machine. If this
# package is absent, knowledge_search degrades cleanly to M45 keyword-only.
sentence-transformers>=3.0

